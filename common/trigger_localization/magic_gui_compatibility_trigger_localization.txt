is_pih_installed_trigger = {
    global = PIH_INSTALLED
    first = PIH_INSTALLED
    third = PIH_INSTALLED
}

is_itpp_installed_trigger = {
    global = ITPP_INSTALLED
    first = ITPP_INSTALLED
    third = ITPP_INSTALLED
}
