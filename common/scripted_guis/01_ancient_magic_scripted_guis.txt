grant_to_mage = {
	is_shown = { # Can be omitted as always true
		always = yes
	}

	is_valid = {
		ROOT = {
			title_province = {
				has_holding_type = city_of_magic_holding
			}
		}
	}

	effect = {
		grant_to_mage_effect = yes
	}
}
